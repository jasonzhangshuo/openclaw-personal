# M2：多 Agent + 多群

## 目标

按职能分工的多 Agent、多群绑定。

## 配置要点

- 在 config 的 `agents.list` 中增加 `brainstorm`、`writer`、`coding` 等，各配 `workspace` 与 `model`。
- `channels.feishu.groups` 与 `bindings` 中为每个群指定 `agentId`；具体规则在前、兜底在后。
- 每个 Agent 独立 workspace、对应模型。

## 验收

- 每群命中正确 agentId；群间会话不串；兜底路由生效。
