---
description: 运行流水、决策记录、NOW 三份文档需及时更新，无需用户每次口头要求
globs: 
alwaysApply: true
---

# CHANGELOG-RUNNING / DECISIONS / NOW / CLAUDE 及时更新

以下四份文档是项目「运行事实」「决策追溯」与「易错点纠正」的单一入口，**在完成相关改动后应主动、及时更新**，无需用户再单独提醒。

## 四份文档的职责

| 文件 | 用途 | 何时更新 |
|------|------|----------|
| **CHANGELOG-RUNNING.md** | 记录「实际做了什么」：配置变更、修复、排查、上线步骤等。按日期追加短句。 | 每次改动运行环境、Gateway、模型、插件、飞书/记忆配置或完成一次修复/排查后，**当天**追加一条日期段 + 条目。 |
| **DECISIONS.md** | 记录关键决策、原因与影响，防止只记得结果不记得为什么。 | 做出会影响「怎么跑、用什么模型、怎么配」的**决策**时，新增一条（日期 + 决策 + 原因 + 影响）。 |
| **NOW.md** | 当前事实入口：目标、系统状态、模型/记忆策略、风险、接下来 3 步、快速恢复清单。 | 当「当前系统状态」「当前风险」「快速恢复清单」或「接下来 3 步」发生变化时，更新对应小节，并刷新文末「最后更新」日期。 |
| **CLAUDE.md** | 记录本仓库中 Claude/人编辑时**容易犯的错误**与**纠正方式**，避免重复踩坑。 | 修完 bug、纠正一次错误行为、或发现「这样改会出事」后，在文件末尾追加一条（现象 + 原因 + 正确做法）。 |

## 执行原则

- **谁改谁记**：你（Agent）在对话中完成的修复、配置变更、决策建议，由你在本次对话内完成对上述三份文档的更新。
- **先冒烟再更新**：做完必要代码/配置修改后，**必须先完成冒烟测试、确认链路打通**，再更新 CHANGELOG-RUNNING / DECISIONS / NOW（以及若属纠正/修 bug 则更新 CLAUDE.md）。未通过冒烟测试不得更新上述文件。
- **同一天多条**：同一天多笔操作可写在 CHANGELOG-RUNNING 的同一日期下；多条决策可写在 DECISIONS 的同一日期下。
- **简短即可**：CHANGELOG 保持短句、可追溯；DECISIONS 保持「决策 / 原因 / 影响」三要素；NOW 只改变动部分和「最后更新」。
- **不重复说**：用户不需要每次都说「记得更新 CHANGELOG / DECISIONS / NOW / CLAUDE」，本规则即约定：相关改动后默认执行更新。

## 示例触发场景

- 修了 Gateway 无法加载 config（如补 LaunchAgent 环境变量）→ CHANGELOG 当日条目 + NOW「当前系统状态」或「快速恢复清单」+ **CLAUDE.md 追加一条**（现象 + 原因 + 正确做法）。
- 决定「Gateway 必须用项目 state/config 且 plist 需带 API key」→ DECISIONS 新增一条。
- 换了模型、改了 fallback、调了 MemOS 参数 → CHANGELOG + 若属决策则 DECISIONS + NOW 中相关小节。
- 纠正了某类重复错误（如 heartbeat 日志乱序、发群话术像标签）→ CLAUDE.md 末尾追加易错点与纠正，便于后续不重复犯。

## 调度相关开发（必读）

涉及任何 heartbeat / cron / 定时任务 / 自动推送 的开发或修改时，**必须先读 `.cursor/rules/openclaw-heartbeat-cron.mdc`**，再动手。每次 OpenClaw 升级后检查该规则是否需要更新。
