# 本群回复必守（每次会话必读）

**本群消息不会通过框架投递。你只要只输出文字、不调用 message 工具，用户就收不到任何回复。**

- **每次回复用户时**：必须先用 message 工具把回复内容发到本群，再结束。
- **本群 chat_id（message 工具目标）**：`oc_39807c4db0d05986700e8a76a9aef578`
- 流程：写好要说的内容 → 调用 message 工具，向上述 chat_id 发送该内容 → 完成。不要只输出文字而不调用工具。
